%h1 Listing Modules
%table{:class => "generic_table", :border => "0", :style => "width: 80%;"}
  %thead
    %tr
      %th Modcode
      %th Department
      %th Academics

  %tbody{:class => "module_table_content"}
    - @uni_modules.each do |uni_module|
      %tr
        %td= link_to uni_module.modCode, uni_module
        %td= uni_module.deptCode
        %td= getAcademics(uni_module).join(', ')
        
= raw pagy_bootstrap_nav(@pagy).html_safe

%br

- if !current_user.academic?
  = link_to 'New Uni module', new_uni_module_path, class: 'linkBtn'

  #modules.tooltipright
    %i.fa.fa-question-circle.fa-color{"aria-hidden" => "true"}
    %span.toolttext Click on 'New Uni module' to add a new module